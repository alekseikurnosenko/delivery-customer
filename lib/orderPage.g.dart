// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'orderPage.dart';

// **************************************************************************
// FunctionalWidgetGenerator
// **************************************************************************

class _Status extends StatelessWidget {
  const _Status(this.order, {Key key}) : super(key: key);

  final Order order;

  @override
  Widget build(BuildContext _context) => _status(_context, order);
}

class _Courier extends StatelessWidget {
  const _Courier(this.order, {Key key}) : super(key: key);

  final Order order;

  @override
  Widget build(BuildContext _context) => _courier(_context, order);
}

class _OrderDetails extends StatelessWidget {
  const _OrderDetails(this.order, {Key key}) : super(key: key);

  final Order order;

  @override
  Widget build(BuildContext _context) => _orderDetails(_context, order);
}

class _SheetContent extends StatelessWidget {
  const _SheetContent(this.order, {Key key}) : super(key: key);

  final Order order;

  @override
  Widget build(BuildContext _context) => _sheetContent(_context, order);
}

class OrderPage extends HookWidget {
  const OrderPage(this.order, {Key key}) : super(key: key);

  final Order order;

  @override
  Widget build(BuildContext _context) => orderPage(_context, order);
}
